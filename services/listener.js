var udp = require('dgram');

var StartListener = function (TypeListener, port) {
    console.log('Ciao! questo è il listener ' + TypeListener + ' che è in esecuzione sulla porta: ' + port);
}






exports.StartListener = StartListener;